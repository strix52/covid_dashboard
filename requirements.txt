# Direct runtime dependencies used by app.py.
# Keep these current with Dependabot; transitive packages are resolved by pip
# from the secure dependency constraints published by Streamlit and its peers.
streamlit==1.60.0
pandas==3.0.5
plotly==6.9.0
